import { notFound } from "next/navigation";import { ActivityScreen } from "@/components/dashboard/activity-screen";const activities=["pre-test","webinar","attendance","post-test","feedback"] as const;export default function Page({params}:{params:{slug:string;activity:string}}){if(!activities.includes(params.activity as typeof activities[number]))notFound();return <ActivityScreen slug={params.slug} activity={params.activity as typeof activities[number]}/>;}
